#ifndef SUPERDIGIT_H
#define SUPERDIGIT_H

#include <string>

using namespace std;

class SuperDigit {
public:
    long long calculateInitialSum(string n);
    long long sumOfDigits(long long n);
    long long compute(long long n);
};

#endif